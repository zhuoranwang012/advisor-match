# legacy/ — archived original RA scripts (provenance only, not in the pipeline)

Put here for reference / reproducibility of how the data was produced:
- 03_extract_keywords.py        (keywords already baked into papers CSV)
- 04_cluster_keyword_summary.py (per-cluster keyword summary)
- matching_pipeline.py          (original master pipeline, professor-level + concept layer)

NOT included on purpose: rerun_comparison.py and the linear-vs-nonlinear section of
matching_pipeline.py — those belong to the separate additive-model project.
